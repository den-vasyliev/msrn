version: 0.0
os: linux
files:
  - source: /
    destination: /opt/msrn
hooks:
  AfterInstall:
    - location: scripts/make
      timeout: 300
      runas: root
    - location: scripts/build
      timeout: 300
      runas: root
  ApplicationStart:
    - location: scripts/start
      timeout: 300
      runas: root
  ApplicationStop:
    - location: scripts/stop
      timeout: 300
      runas: root